apiVersion: batch/v1
kind: Job
metadata:
  name: net-test
spec:
  template:
    spec: 
      containers:
      - name: tester
        image: nicolaka/netshoot:latest
        command: ["sleep", "3600"]
      restartPolicy: OnFailure
  ttlSecondsAfterFinished: 10
